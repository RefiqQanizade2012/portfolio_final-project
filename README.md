# final project

# Советчик по выбору карьеры и профессии

Демо‑телеграм‑бот для подростков и людей, желающих сменить профессию. Быстрое и простое взаимодействие: короткие опросы, понятные карточки с рекомендациями и кнопки для действий.

**Основное:**

* Персонализированные рекомендации на основе ответов пользователя.
* Взаимодействие через текст и кнопки (inline/меню).
* Подача информации кратко и понятно для целевой аудитории.
* Демо‑версия для тестирования функциональности перед финальным запуском.

**Технологии (коротко):**

* **Библиотека для Telegram:** `pyTelegramBotAPI` (та же, что и в предыдущих проектах).
* **База данных:** SQL (PostgreSQL для продакшна, SQLite для демо).
* **Язык:** Python 3.10+.

**Что в демо:** бот с опросом, генерация простых рекомендаций, меню для навигации, тестовые сценарии. Бот использует `pyTelegramBotAPI` и подключается к SQL‑базе.

**Запуск (минимально):**

1. Клонировать репозиторий.
2. Создать виртуальное окружение и установить зависимости: `pip install -r requirements.txt` (в `requirements.txt` добавить `pyTelegramBotAPI`, `SQLAlchemy` и драйверы для выбранной СУБД).
3. Заполнить `config.py` с `TELEGRAM_TOKEN` и `DATABASE_URL`.
4. Инициализировать базу (для демо — скрипт `init_db.py` создаст таблицы в SQLite).
5. Запустить бота: `python bot.py` (long polling). Для webhook — настройка через сервер.

---

## Первоначальная (полная) версия

> *Демо-версия телеграм‑бота, который помогает подросткам и людям, которые устали от своей работы, найти новые и интересные пути для развития карьеры.*

---

## Кратко

Мы — молодой стартап, задача — сделать дружелюбного и умного бота в Telegram, который помогает пользователям:

* получить персонализированные рекомендации по профессии и развитию;
* пройти лёгкую «ориентацию» через диалоги, тесты и интерактивные меню;
* ознакомиться с реальными шагами (курсы, навыки, первые вакансии).

---

## Цели демо-версии

1. Отработать базовую логику персонализации (анкетирование → профиль → рекомендации).
2. Реализовать взаимодействие через текст и кнопки/меню.
3. Показать удобный формат выдачи информации для подростков (короткие блоки, иконки, шаги).
4. Описать формат базы данных и показать пример наполнения.

---

## Основные требования (реализуемые в демо)

1. **Адаптивность:** бот подстраивается под ответы пользователя и запоминает профиль.
2. **Мультимодальность интерфейса:** текстовые сообщения + inline/reply‑кнопки + меню.
3. **Понятность контента:** карточки рекомендаций, краткие чек‑листы, шаги «что делать дальше».
4. **База данных:** лёгкая структура, удобная для расширения (описание формата — ниже).

---

## Инструменты для реализации:

> * **Telegram lib:** `pyTelegramBotAPI`.
> * **СУБД:** PostgreSQL или SQLite (для демо). Драйверы: `psycopg2-binary` для Postgres, `aiosqlite`/встроенный `sqlite3` для SQLite.
> * **ORM (опционально):** SQLAlchemy.
> * **Хранилище:** файлы / S3 для медиа (иконки, изображения профессий).
> * **Docker:** для упрощённого развёртывания демо.
> * **CI/CD:** GitHub Actions (опционально).

---

## Проект состоит из:

> 1. **Бот (backend):** логика диалогов (pyTelegramBotAPI), API к базе, генерация рекомендаций.
> 2. **База данных:** SQL таблицы пользователей, профилей, профессий, шаблонов рекомендаций.
> 3. **Контент‑редактор (описание):** простая структура CSV/JSON, которую команда сможет дополнять.
> 4. **Демо‑скрипты:** сценарии тестирования, набор примеров пользователей.
> 5. **Документация:** README (этот файл), описание формата БД и инструкции по запуску.

---

## Рекомендации по зависимостям (`requirements.txt`)

```
pyTelegramBotAPI>=4.0.0
SQLAlchemy>=1.4
psycopg2-binary>=2.9   # если используете Postgres
aiosqlite>=0.17        # если хотите async sqlite
python-dotenv
```

---

## UX / сценарии взаимодействия (демо)

1. **Привет:** бот здоровается и предлагает пройти короткий опрос (3–6 вопросов).
2. **Опрос:** возраст → образование → 3‑4 интереса / предпочтения → уровень навыков.
3. **Профиль:** бот формирует карточку «Твой профиль» с кнопками: "🔁 Перепройти", "📚 Посмотреть курсы", "📌 Сохранить вакансии".
4. **Рекомендации:** 3–5 профессий карточками: заголовок, 1‑строчный текст, 2‑3 шага и кнопка "Подробнее".
5. **Deep‑view:** при нажатии — подробная страница профессии, чек‑лист навыков, ссылки на курсы/ресурсы.

---

## Что делать в демо‑версии (шаги)

1. Развернуть бот локально.
2. Заполнить тестовые записи в базе (2–3 профиля, 6–8 профессий).
3. Пройти сценарии теста: подросток, сменивший карьеру, человек без опыта.
4. Собрать обратную связь и улучшить шаблоны вывода.

---
